package main

import (
	"fmt"
)

var pl = fmt.Println

func main() {
	/*
				var arr1 [5]int
				arr1[0] = 1
				arr2 := [5]int{1, 2, 3, 4, 5}
				fmt.Println("Index 0: ", arr2[0])
				fmt.Println("Array length: ", len(arr2))
				for i := 0; i < len(arr2); i++ {
					fmt.Println(arr2[i])
				}
				for i, v := range arr2 {
					fmt.Println(i, ":", v)
				}
				arr3 := [2][2]int{
					{1, 2},
					{3, 4},
				}
				for i := 0; i < 2; i++ {
					for j := 0; j < 2; j++ {
						fmt.Print(arr3[i][j])
					}
					fmt.Println()
				}

			aStr1 := "abcde"
			rArr := []rune(aStr1)
			for _, v := range rArr {
				fmt.Println(v)
			}
			byteArr := []byte{'a', 'b', 'c'}
			bStr := string(byteArr[:])
			fmt.Println(bStr, reflect.TypeOf(bStr))

		sl1 := make([]string, 6)
		sl1[0] = "fame"
		sl1[1] = "money"
		sl1[2] = "love"
		sl1[3] = "fairness"
		sl1[4] = "truth"
		pl("Slice Size: ", len(sl1))
		for i := 0; i < len(sl1); i++ {
			pl(sl1[i])
		}
		for _, x := range sl1 {
			pl(x)
		}
		sArr := [5]int{1, 2, 3, 4, 5}
		sl3 := sArr[0:2]
		pl(sArr[1:4])
		pl(sArr[:3])
		pl(sArr[2:])
		sArr[0] = 10
		pl(sl3)
		sl3[0] = 1
		pl(sArr)

		sl3 = append(sl3, 12)
		pl(sl3)
		pl(sArr)

		sl4 := make([]string, 6)
		pl(sl4)
	*/

}
